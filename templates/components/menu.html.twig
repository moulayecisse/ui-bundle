{# templates/components/Ui/menu.html.twig #}
{% props
    class = ''
%}

{% set class = ('flex flex-col gap-2 mb-6 w-full ' ~ class)|tailwind_merge %}

<div
        {{ attributes }}
        data-controller="cisse--ui-bundle--menu"
        class="{{ class }}"
>
    {% block content '' %}
</div>